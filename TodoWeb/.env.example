# 复制为 .env.development 可本地覆盖开发环境变量

# 开发时跳过登录（仅 DEV 生效，生产构建不受影响）
# true = 直接进入主界面，方便只跑前端调试
# VITE_DEV_SKIP_LOGIN=true

# 接口基础地址。开发时留空即可，Vite 会代理 /api 到后端（无跨域）
# 生产/桌面打包时若后端单独部署，可设为完整地址，如 https://api.example.com
# VITE_API_BASE_URL=
